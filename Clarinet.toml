[project]
name = "universal-feed"
authors = ["Universal Feed Team"]
description = "A decentralized content and subscription management protocol"
telemetry = false
requirements = []
[contracts.universal-feed]
path = "contracts/universal-feed.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
